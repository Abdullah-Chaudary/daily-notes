# Daily Notes

A personal log of things I work on and learn each day. Every entry is
timestamped and appended to `journal.md`, which builds a searchable record of
small experiments, fixes, and study notes over time.

```
journal.md   <- the running log (updated daily)
.github/     <- workflow that maintains the log
```
